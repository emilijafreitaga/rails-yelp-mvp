<h2>Add a new review</h2>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, as: :integer, placeholder: "Between 0 and 5" %>
    <%= f.submit "Add a review", class: "btn btn-primary" %>
  <% end %>
</div>

<%= link_to '< Back', restaurants_path %>
